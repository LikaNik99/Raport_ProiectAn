@model PayViewModel
@{

}

<section class="vh-100">
    <div class="container py-5 h-100">
        <div class="col-md-8 mx-auto rounded border p-3">
            <h2 class="text-center mb-5">Plătește serviciul</h2>

            <form method="post" enctype="multipart/form-data">
                <input type="hidden" name="EntityService.Id" value="@Model.EntityService.Id" />
                @if (Model.TranId > 0)
                {
                    <div class="row mb-3">
                        <label class="col-sm-4 col-form-label">Numărul facturii</label>
                        <div class="col-sm-8">
                            <input class="form-control" readonly value="@Model.TranId" />
                        </div>
                    </div>
                }
                <div class="row mb-3">
                    <label class="col-sm-4 col-form-label">Serviciul</label>
                    <div class="col-sm-8">
                        <input class="form-control" readonly value="@Model.EntityService.Name" />
                    </div>
                </div>
                @if (Model.TranId == 0)
                {
                    <div class="row mb-3">
                        <label class="col-sm-4 col-form-label">Numărul de cont</label>
                        <div class="col-sm-8">
                            <input asp-for="ProviderId" class="form-control" />
                            <span asp-validation-for="ProviderId" class="text-danger"></span>
                        </div>
                    </div>
                }
                else
                {
                    <div class="row mb-3">
                        <label class="col-sm-4 col-form-label">Numărul de cont</label>
                        <div class="col-sm-8">
                            <input class="form-control" readonly value="@Model.ProviderId" />
                        </div>
                    </div>
                }
                @if(Model.TranId == 0)
                {
                    <div class="row mb-3">
                        <label class="col-sm-4 col-form-label">Suma</label>
                        <div class="col-sm-8">
                            <input asp-for="Amount" class="form-control" step="0.01" min="0.01" />
                            <span asp-validation-for="Amount" class="text-danger"></span>
                        </div>
                    </div>
                }
                else
                {
                    <div class="row mb-3">
                        <label class="col-sm-4 col-form-label">Suma</label>
                        <div class="col-sm-8">
                            <input class="form-control" readonly value="@Model.Amount" />
                        </div>
                    </div>
                }
                
                @if (TempData["Message"] != null)
                {
                    var alertType = TempData["AlertType"]?.ToString() ?? "info";
                    <div class="alert alert-@alertType alert-dismissible fade show" role="alert">
                        <strong>@TempData["Message"]</strong>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                }
                <div class="row">
                    @if(Model.TranId == 0)
                    {
                        <div class="offset-sm-4 col-sm-4 d-grid">
                            <button type="submit" class="btn btn-primary">Plătește</button>
                        </div>

                        <div class="col-sm-4 d-grid">
                            <a class="btn btn-outline-primary" asp-controller="Home" asp-action="Services" role="button">Înapoi</a>
                        </div>
                    }
                    else
                    {
                        <div class="col-sm-12 d-grid">
                            <a class="btn btn-outline-primary" asp-controller="Home" asp-action="Services" role="button">Înapoi</a>
                        </div>
                    }
                </div>
            </form>
        </div>
    </div>
</section>